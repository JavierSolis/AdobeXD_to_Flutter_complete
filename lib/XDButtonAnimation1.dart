import 'package:flutter/material.dart';

class XDButtonAnimation1 extends StatelessWidget {
  XDButtonAnimation1({
    Key key,
  }) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: const Color(0xffffffff),
      body: Stack(
        children: <Widget>[],
      ),
    );
  }
}
